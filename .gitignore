# Gapcoin reference source (not part of this miner)
gapcoin_src/

# Build output
bin/
build/
src/*.o
tests/test_rpc_json

# Logs / runtime data
logs/
miner.log
perf.data
*.log

# Fleet record dumps (can reach 100+ MB — never push to GitHub)
gap_hunt_records_f1.txt
gap_hunt_records_f2.txt

# Best-known-merit table (regenerated by scripts/update_merits.sh — local only)
data/prime_gap_merits.txt

# Backup archives
*.tar.gz
*.tar.bz2
*.zip

# Temp / debug block dumps
gbt.json
/tmp/gap_miner_block_*.bin
/tmp/gap_miner_block_*.hex

# Editor / OS
.vscode/
*.swp
*~
.DS_Store
gapminer_records.log*
gapminer_pool_records.log*
data/gap_hunt_records.txt
data/gap_hunt_state.txt
data/gap_hunt_records.old
