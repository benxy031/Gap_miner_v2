# Closed Fingerprints — routes that are dead until a causal axis changes

Anti-regression registry for gapminer_v2 (mining + GAP_HUNT).  A **closed
fingerprint** is a route that was attempted, measured or proved, and failed
for an identified reason.  The same route must not be retried until one of
its causal axes changes (`data`, `hardware`, `representation`, `boundary`,
`unit of work`, `bottleneck`, `invariant`, `verifier`, `composition`,
`downstream cost`).  A rename, new language, or toy-size change does not
reopen an entry.

Claim labels used in logs and scripts (adopted from the record-grade witness
taxonomy): `EXACT_GAP`, `FIRST_KNOWN_OCCURRENCE`, `FIRST_OCCURRENCE`,
`MAXIMAL_GAP`, `MERIT_RECORD`, `NETWORK_ACCEPTED`.  Our tools only ever emit
`EXACT_GAP` (validator) and `FIRST_KNOWN_OCCURRENCE` (watcher/record log);
the stronger labels require coverage or network evidence we do not have.

## GAP_HUNT

| Fingerprint | Verdict / first cost | Evidence | Reopen trigger |
|---|---|---|---|
| QUARTER_CLASS two-pass scan for record hunting | EXACT but falsified: visible gaps inherit the σ-tail (mean merit ≈ 8), containment trigger fires ~1.3×/window at m=15; CPU resolution ~40 ms → 46 win/s vs 893 full-class | `GAP_HUNT_QUARTER` env (off), parity test identical, commit `7a0e30e` | a hidden-resolution path ~300× cheaper than mini-sieve 10k + GMP MR |
| Cover re-optimization beyond lex-m30 (74 primes) | lex/run/blocks objectives all converge ~1198 survivors vs production 1141; entropy bound with Σ1/p ≈ 2.04 leaves ~1000+ survivors | gen_crt probes Sep 2 | a new objective that provably lowers survivor count below ~900, or a different prime budget |
| "Gap factory" (2–3 survivors to force merit-31 gaps every window) | impossible: 74 prime moduli cannot cover [1,15867) up to 3 survivors (set-cover entropy); prime-moduli covering systems cannot cover Z (Euclid) | analysis Sep 2 | a cover with Σ 1/p ≫ 2.4 (hundreds of primes) at negligible mark cost |
| Synthetic gap construction (no search) | primality of endpoints cannot be constructed without primality search; AKS is a test, not a birth law | theorem cards G0 (sibling corpus) | an identity that co-defines the relation before the integers exist |
| GPU_SIEVE_PAIR (mining experiment, kept for reference) | −58% win/s: monolithic 2-window kernel starves extract/MR kernels | README GPU table | scheduler evidence the mark kernel no longer serializes |
| Same-family fixed-budget mono-CRT retuning for equal M30 yield | current mono-CRT ~87× below M25 per row; retuning cannot change the family's p0/row | sibling corpus R445 `m30` verdict + our lex probe | a different family/boundary, not new residue choices |

## GapMiner (mining pipeline)

| Fingerprint | Verdict / first cost | Evidence | Reopen trigger |
|---|---|---|---|
| shift998 m40 for live merit | no ≥16-merit gaps on the pipeline; MR cost (bits/64)² dominates | production A/B (May–Jun) | CGBN width for 1280-bit candidates measurably cheaper |
| Blocks/run objectives as σ boost | runs never approach merit scale; σ is conditioning-driven, not run-driven | `gen_crt` -B/-d tools + falsification notes | a score that correlates with exact-gap yield, not run count |
| CGBN sliding-window tuning (WIN_BITS=4, binary sq-and-mul, maxrregcount) | WIN_BITS=3 already optimal at 255 regs; all alternatives spill more | memory: GPU Fermat kernel notes | a different AL/NL geometry or SM arch |
| Producer-consumer overlap at shift=512 | 30% window queue drops (sieve outproduces GPU) | memory: monolithic 4T best | a consumer ≥ 2× faster |
| `GPU_SIEVE_PAIR` (see above) | −58% | README | — |
| Smart-scan tail-skip with per-pair hidden resolution at record thresholds | resolution cost > MR savings (same trigger-rate law as QUARTER_CLASS) | quarter-class parity/benchmark | — |
| GAP_HUNT_JUMP (Kehrig-style serial per-window CGBN walk) | exact (parity 299/299 at 507, 411/411 at 1017) but 6.3x slower (23.5 vs 147 win/s): CGBN cooperative test latency ~10 ms under load, unhideable by 32-window parallelism; 1017 is mark/extract-bound anyway (143 win/s at m=8 vs 147 at m=18) | new_src/gpu/gpu_fermat.cu (cgbn_jump_scan_kernel_t), gap_hunt.c GAP_HUNT_JUMP | a per-thread low-latency MR at AL=20 (or >500 concurrent windows) |
| Raising `--gap-hunt-min-merit` to hit records sooner | **REFUTED as a no-op (2026-09-17)**: the invariance assumed a pure exponential tail. Measured A/B on one walk (shift507 lex, 30 s arms, timing on): merit 18 -> 2404 win/s, 1.39e-5 gaps/win, 121 gaps/h; merit 10 -> 1460 win/s (-39%), 1.47e-2 gaps/win, 78100 gaps/h. Using the size-exact kernel, p_record falls only 46x (2.864e-05 -> 6.196e-07) while the gap rate rises 645x => **records/h = 14x BETTER at threshold 10** (0.0484 vs 0.00347). The reason the invariance fails is that the tail is lighter than exponential at depth (measured sigma 1.29 at M0=14 -> 0.858 at M0=19), so p_record decays slower than the gap count grows | `scripts/record_rate_model.py --m0 10|18` + the hunt A/B in repo memory, docs/RECORD_RATE_MODEL.md §4 | a walk whose sigma is FLAT (a true exponential tail) would restore the invariance — re-measure before assuming either direction |
| Optimising covers for σ (the "cover shapes the tail, so shape the cover" lever) | the near-threshold σ IS cover-driven (same-size shift1017 strong vs lex at merit 18: σ 1.2741±0.0098 vs 1.3621±0.0104 = 6.2σ) but it does NOT transfer to the record depth: separation ≤0.6σ at M0 21-23 and −2.8σ at M0=24 (n=126/209), and the observed record counts are indistinguishable (13 vs 10; best merit 29.389 vs 29.038, ~0.6σ). The two tails cross | `scripts/tail_compare.py data/gap_hunt_records_f1.txt data/gap_hunt_records_f2.txt`, docs/RECORD_RATE_MODEL.md §8 | a σ fit AT the record depth (M0 >= 24) with n >= 1000 per cover, i.e. ~10x more data per arm, or a pooled multi-cover estimator |
| Folding the frontier slope into σ (`sigma_eff`) to sharpen the record-rate model | falsified twice: (1) the form was INVERTED — for μ(g)=a−b·g the margin grows with merit at rate (1+bL)>1, so σ_D = σ·(1−L·dμ/dg), not σ/(…); (2) even corrected it double-counts, since p_record already evaluates μ(g) at every size. No single rescale fits both covers: strong 5.7 → 4.9 (divide) or 12.8 (multiply) vs observed 13; lex 9.1 → 6.3 or 19.3 vs observed 10 | `scripts/record_rate_model.py` (slope kept as DIAG only), docs/RECORD_RATE_MODEL.md §8.5b | a residual model error that a pointwise sum cannot express, e.g. an empirically measured non-exponential tail shape (stretched exp / GPD) at the record depth |
| Duplicate gap emissions in the GAP_HUNT output | CHECKED, NOT A BUG: two different gaps of the same size print identically because merit is a function of the size at constant L, and 2453 sizes repeat in 755202 local gaps. Verified on 989k gaps: 0 identical lines and 0 repeated start primes. The ambiguity was in `gap_hunt_stats.py`, which discarded the start prime — it now reports `integrity: lines/distinct_starts/duplicate_lines` and marks repeated sizes with an `xN` suffix | `scripts/gap_hunt_stats.py data/gap_hunt_records_258.txt`, 2026-09-17 | a report showing `duplicate_lines > 0`, which would be a real re-emission |
| Periodic `save_state()` claiming in-flight windows | HARDENED, effect not observed: the save now persists `min(next_k, oldest active flight's k0)` so a hard kill cannot skip windows that were filled but never collected. Instrumented run (16/16 ticks, shift507, batch 64) shows `A(act=0 n=0) B(act=0 n=0)` at every save, i.e. `windows == next_k` — in this loop order the state was already exact, so nothing changed in practice | `new_src/gap_hunt.c` (GAPDEBUG watermark line), 2026-09-17 | a configuration where a flight IS active at the tick (loop-order change, skipped collect, larger batch vs tick interval) |
| Exponential merit tail for deep (record-depth) extrapolation | REJECTED for the lex cover, ADEQUATE for the strong cover. Powered fleet data (16893/17275 gaps at threshold 18, held-out M0=21/23/25): f1 strong -- exp best LL (-2329.20), Delta-LL vs gpd = 1.78 (not significant), mean excess flat 1.30/1.20/1.31; f2 lex -- gpd wins by Delta-LL = 8.77 (p~3e-5), exp over-predicts the M0=25 count 101.9 vs 59 (z=-4.25), mean excess falls 1.293->1.212 | `scripts/tail_shape.py ... --u-fit 18 --u-test 21,23,25`, docs/RECORD_RATE_MODEL.md §9.7 | a cover that changes the sign of the effect, or a joint (size, cover) shape model |
| Finite merit ceiling (GPD xi<0 endpoint at merit 26-45) | REFUTED as stated: that endpoint came from a u=16 fit on n=1509. With 10x more data at the record-relevant threshold the endpoint moves to ~69 (lex) / effectively unbounded (strong) and abs(xi) shrinks ~3x (from -0.084 to -0.018/-0.0275), i.e. far above the operating range merit <= 30 | `scripts/tail_shape.py` on the live fleet files, docs/RECORD_RATE_MODEL.md §9.7 | a direct measurement of a merit ceiling at >=10x the current deep sample, or a physical argument for boundedness |
| Fleet shift move for a record-rate win | landscape is flat near the optimum: cheapest reachable target over shifts 200..1050 needs merit 24.2381 and is hit at shifts 1000 and 1017 (the shipped fleet value); alignment is worth ~4x across 900..1050 but only ~7% at the top | `scripts/record_rate_model.py --shift-scan 200:1050:25`, docs/RECORD_RATE_MODEL.md §5 | a new table snapshot that moves the cheapest target to a different reachable band, or a measured per-shift walk-rate advantage that dominates the merit alignment |
| **GAP_HUNT: full-scan path at low report thresholds** | **RESOLVED (2026-09-17) — it was BUILD HYGIENE, not gap logic.** The real cause: `build/new_src/gpu_adapter.o` was compiled **2026-09-04**, i.e. *before* `GPU_ADAPTER_MAX_BATCH` was raised 160000 → 320000 in `gpu_adapter.h` (2026-09-15), and the Makefile did not track headers. So `gpu_fermat_init()` built the context with `max_batch = 160000` while every freshly compiled caller (`gap_hunt.o`, `worker_gpu.o`) verified its batch against the 320000 macro — and `gpu_fermat_submit*()` clamped `count > max_batch` **silently**. Every submit therefore lost the candidates after index **160000** (their MR results stayed zero ⇒ genuine primes read as composite), which is exactly the observed wall: cumulative-candidate cutoff, not a window index (sparse windows ~1780/window: alive at i=88, dead at i=91; dense ~2780/window: alive at i=54, **partially** alive at i=57 (54 of ~95 primes), dead at i=58 — cutoff ≈160000 in both). Fixed by (a) `make clean` + full rebuild, (b) `-MMD -MP` + `-include` of the generated `.d` files in the Makefile, (c) `gpu_fermat_max_batch()` so callers size their batches from the CONTEXT, (d) a loud clamp warning instead of a silent one, (e) chunking in `worker_gpu.c` at the real cap. Verified after the fix: the four previously missing gaps (8544, 8570, 8616, 8814) come back, the plain scan at batch 128 gives **15/15 = byte-identical to the jump2 set**, and `MINING_JUMP2_VERIFY=1` stays clean (388 flights, 0 bad). The evidence trail (real gaps, strict verifier PASS, batch-size dependence) is preserved above because it is what localized the cause | same A/B + `/tmp` dumps, `GHDBG_SUBMIT=1` (`count=160000 max_batch=160000` before, `count=292020 max_batch=320000` after) | a *logic* bug of this shape would need its own row: if a batch ever exceeds the context cap again, the loud `[gpu_fermat] WARNING: ... candidates NOT tested` line names the reason |
| Stale object carrying a superseded constant (build hygiene) | **FOUND (2026-09-17), closed by tooling**: the Makefile now emits and includes header dependencies (`-MMD -MP` + `-include $(shell find $(BUILD_DIR) -name '*.d')`), so changing any header rebuilds its users; `gpu_fermat_max_batch()` exposes the context's real per-call cap so callers size batches from the CONTEXT instead of a macro; the silent clamp became a loud warning; `worker_gpu.c` chunks at the real cap. Evidence of the class: `GHDBG_SUBMIT=1` printed `count=160000 max_batch=160000` before the rebuild and `count=292020 max_batch=320000` after. Cost when it happens: every candidate beyond the (stale, smaller) cap is silently never tested | Makefile, `gpu_fermat.{h,cu}`, `gap_hunt.c`, `worker_gpu.c`, `docs/BUILD_HYGIENE_INCIDENT.md` | a build path that bypasses CFLAGS/NVCC_FLAGS for some object (hand-written compile line, external script, copied .o) would reintroduce the class |
| Chain flight batch above 128 windows (`MINING_JUMP2_BATCH_MAX` 128 → 256) | **RESOLVED AS A WIN (2026-09-18)** — the 2026-09-17 "K=256 silently corrupts the emitted set" verdict was WRONG and the error was in the VERIFIER: `MINING_JUMP2_VERIFY` replayed the flight's full scan in ONE `gpu_fermat_submit_device` call, so at K=256 it submitted 256 x candidates-per-window (455k at shift512, 685k at shift1017) and the MR staging cap (320000) truncated it — the verifier then compared the chain against an untested tail and reported exactly those tail windows as bad (`bad_windows=75..83`). Rebuilt with the replay sliced at the context cap, K=256 verifies clean (shift512: 214 flights; shift1017: 67 flights; all `bad_windows=0 bad_pairs=0`) and is FASTER: **+17% at shift512** (5264 → 6113 win/s, 119.7/122.6 → 141.4/143.3/141.5 mH/s) and **+19.4% at shift1017** (1498 → 1788 win/s). `MINING_JUMP2_BATCH` default is now 256 (the chain's own submits are K x chunk = 8192 candidates, three orders below the cap) | `new_src/worker_gpu.c` (`MINING_JUMP2_BATCH_MAX`, `crt_fused_chain_verify` slicing), README batch row, 2026-09-18 | — |
| GAP_HUNT batch above 64 windows (`GAP_HUNT_BATCH_MAX` 128 → 512, default 64 → 512) | **RESOLVED AS A WIN (2026-09-18)** — same mechanism as the miner chain batch: a round pays a fixed per-launch floor in the CGBN MR kernel, so more windows per round = cheaper rounds. Measured on the quiet dev GPU with the mode-aware bound (jump2 on, min-merit 19, fixed k range 0..65535, emitted sets identical across K): shift507 **2452 (K=64) → 3156 (128) → 3601 (256) → 3824 win/s (512) = +56%**; shift1017 **881 (128) → 1001 (256) → 1172 win/s (512) = +33%**. Non-vacuous parity at min-merit 16 over k∈[0,131072): **15 gaps, set-identical** at K=512 vs K=64 (3434.6 vs 2291.8 win/s, **+49.9%**). Cost is device memory, `2 × odd_interval_size × K × limbs × 8 B`, printed by the banner as `vram_est=<MB>`: **2384 MB** at shift507 K=512 and **6407 MB** at shift1017 K=512 (≈ 7.3/8 GB actually used with the sieve contexts + adapter + display, so at high shift K=512 is at the edge of an 8 GB card; use 256 there, 12 GB cards are fine). Shift1017 default-512 parity re-checked: 2 gaps, set-identical vs K=128, 1078.6 vs 845.6 win/s (+27.5%), no alloc failure on the 8 GB dev card | `new_src/gap_hunt.c` (default 512, cap 512, mode-aware `cumcap`: the full-scan head still clamps per window to `gpu_fermat_max_batch()`), README `GAP_HUNT_BATCH` row, 2026-09-18 | a card with enough VRAM for K=1024 (≈ 8.6 GB at shift1017 — not viable on 8-12 GB), or free-VRAM-driven auto-clamp (`cudaMemGetInfo` is not wired anywhere in this repo yet) |
| A VERIFIER with its own capacity limit issuing a false "corrupt" verdict (measurement apparatus as the bug) | **FOUND (2026-09-18)**: two independent false verdicts in one week came from the *measuring* code, not the measured code — (1) the K=256 chain "corruption" above (verifier replay truncated by the MR cap), and (2) the 2026-09-17 GAP_HUNT "full scan misses 27% of gaps" (stale `gpu_adapter.o` capped the context at 160000 while callers used 320000; the full-scan run was the one silently truncated, and the chain was fine). Lesson: before concluding that a path is wrong, check that the *comparison* ran with enough capacity and the same constants; a verifier that submits everything in one shot inherits every staging cap | `docs/BUILD_HYGIENE_INCIDENT.md` §3-4; CLOSED_FINGERPRINTS rows above | a third occurrence would justify a standing rule: every verifier must assert its own batch fits `gpu_fermat_max_batch()` before comparing |
| More CRT rows per nonce beyond ~64 (`CRT_ROWS_BATCH`) | SATURATED: measured curve at shift507 `p74_lex_m30` (K=128 chain, quiet GPU, 45 s points) rows 0 -> 4059, 1 -> 4169, 8 -> 5055, 32 -> 5239, 64 -> 5557, 92 -> 5547 win/s. Nothing above 64 rows is available | measured 2026-09-17 | a cost model in which the per-nonce alignment/CRT share is larger than today (e.g. a much cheaper MR kernel) |
| Treating the shift512 `p75` cover as a 75-vs-74-prime covering question | DEFECT, not a lever: `shift512_p75_lex_m30.txt` was generated with `ctr_bits=3` (log2 P = 509.0 of the 512-bit shift) so it has only **7** rows per nonce, while the standard m23 files use `ctr_bits=8` (shift509 `p74` -> 362 rows, shift507 `p74` -> 92). Cost at K=128, quiet GPU, identical 120.3 MR tests/window: 4809/5047 win/s (7 rows) vs **5288** (shift509 `p74_strong_m32`) and **5229/5464** (freshly generated 73-prime `--ctr-bits 20`) => **+8.5..9.8%** for a row-rich cover at the same shift/difficulty. Impact is limited to benchmarks and README examples: the production fleet configs (507/450/1017) are all ≥ 92 rows | cover headers (`n_primes`/`shift` vs `gen_crt`'s `ctr_bits`), row curve, and the 2026-09-17 A/B | a shift where no `ctr_bits >= 6` cover fits the prime budget (then the tradeoff must be re-measured) |
| HALF_CLASS / QUARTER_CLASS together with the mining chain | REGRESSION in chain mode at shift512 (quiet GPU): HALF 3362 vs 3902 full-class (-14%), QUARTER 993 (-75%). The older +14.6% / +10.7% numbers were measured WITHOUT the chain | README env table + 2026-09-17 idle-GPU A/B | a chain in which hidden-class resolution is chain-integrated (today it is an on-demand side path the frontier cut cannot amortize) |

## House rules

1. Before starting any experiment, check this file; a matching fingerprint must
   name the causal axis that changed, or the experiment is a regression replay.
2. Every new closed entry records: objective, representation, authenticated
   facts, first compensating cost, evidence (commit/file), reopen trigger.
3. A script bug found while interpreting an experiment is part of the evidence:
   record it, fix it, and regression-check the old path (2026-09-17:
   `tail_compare.py` computed its verdict below the data's report threshold,
   turning a real 6.2σ into 0.9σ; and `record_rate_model.py` used a merit-bin
   half-width of 0.5 where the even-gap lattice requires 1.0, making every
   p_record and E[records] exactly 2x low — found by a Monte-Carlo check on the
   real table, exposed by `--bin-half`).
3. Closing an entry is a verdict on the route, not on the person or idea;
   dormant routes stay listed with their trigger.
